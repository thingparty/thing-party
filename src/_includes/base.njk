<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% if title %}{{ title }} — {% endif %}{{ site.title }}</title>
  <link rel="stylesheet" href="{{ '/css/style.css' | url }}">
  <link rel="alternate" type="application/atom+xml" title="{{ site.title }}" href="{{ site.url }}{{ '/feed.xml' | url }}">
</head>
<body>
  <div class="site-wrapper">
    <nav class="sidebar">
      <a href="{{ '/' | url }}" class="site-name">{{ site.title }}</a>
      <ul class="nav-links">
        <li><a href="{{ '/about/' | url }}">About</a></li>
        <li><a href="{{ '/blog/' | url }}">Blog</a></li>
        <li><a href="{{ '/stories/' | url }}">Stories</a></li>
        <li><a href="{{ '/paintings/' | url }}">Paintings</a></li>
        <li><a href="{{ '/contact/' | url }}">Contact</a></li>
      </ul>
      <div class="nav-footer">
        <a href="{{ site.instagram }}">Instagram</a>
        <p>&copy; {{ site.year }}</p>
      </div>
    </nav>
    <main class="content">
      {{ content | safe }}
    </main>
  </div>

  {% if extraJs %}
  <script src="{{ extraJs | url }}"></script>
  {% endif %}
</body>
</html>
